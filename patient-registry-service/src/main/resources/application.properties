# ----- identity & port -----
spring.application.name=patientregistrychart
server.port=8086

# ----- datasource / jpa (as you already had) -----
spring.datasource.url=jdbc:mysql://localhost:3306/patient_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Shlok@21
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ----- eureka -----
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.hostname=localhost
eureka.instance.prefer-ip-address=true

# ----- jwt (must match Auth exactly) -----
jwt.secret=caretrackSecretKeyForJwtAuthenticationhjgihjrugoijeo
jwt.expiration=3600000   # for reference; validation uses token's exp

# ADD THESE at the BOTTOM of your existing file
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8081
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8081/api/auth/public-key
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
logging.level.com.caretrack.patient=DEBUG
logging.level.org.springframework.security=DEBUG